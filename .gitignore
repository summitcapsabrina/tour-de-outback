# Secrets — never commit real environment values
.env
.env.local
.env.*.local
.runtimeconfig.json

# Firebase
.firebase/
firebase-debug.log
firebase-debug.*.log
functions/node_modules/

# OS
.DS_Store

# Internal marketing context (product-marketing skill) — keep out of the public repo
.agents/
